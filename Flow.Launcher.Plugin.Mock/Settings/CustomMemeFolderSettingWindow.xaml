<Window x:Class="Flow.Launcher.Plugin.Mock.Settings.CustomMemeFolderSettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Flow.Launcher.Plugin.Mock.Settings"
        Title="Custom Meme Folder Settings"
        Width="550"
        d:DataContext="{d:DesignInstance vm:CustomMemeFolderViewModel}"
        Background="{DynamicResource PopupBGColor}"
        Foreground="{DynamicResource PopupTextColor}"
        ResizeMode="NoResize"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32"
                      ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}" />
    </WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="80" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="4"
                            Click="OnCancelButtonClick"
                            Style="{StaticResource TitleBarCloseButtonStyle}">
                        <Path
                            Width="46"
                            Height="32"
                            Data="M 18,11 27,20 M 18,20 27,11"
                            Stroke="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
                            StrokeThickness="1">
                            <Path.Style>
                                <Style TargetType="Path">
                                    <Style.Triggers>
                                        <DataTrigger
                                            Binding="{Binding Path=IsActive, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"
                                            Value="False">
                                            <Setter Property="Opacity" Value="0.5" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Path.Style>
                        </Path>
                    </Button>
                </Grid>
            </StackPanel>
            
            <StackPanel Margin="26,12,26,12">
                <Grid>
                    <StackPanel>
                        <StackPanel Margin="10,0,10,15">
                            <TextBlock 
                                Margin="0,0,0,0" 
                                FontSize="20" 
                                FontWeight="SemiBold"
                                Text="add/edit custom meme folder ^-^" 
                                TextAlignment="Left" />
                        </StackPanel>
                        <StackPanel Margin="10,0,10,15">
                            <TextBlock FontSize="14" Text="please fill in the details for the custom meme folder :)"
                                       TextAlignment="Left" TextWrapping="WrapWithOverflow" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="500">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <TextBlock 
                                    Grid.Row="0" 
                                    Grid.Column="0" 
                                    Margin="10"
                                    HorizontalAlignment="Stretch" 
                                    VerticalAlignment="Center" 
                                    FontSize="14"
                                    Text="Keyword" />
                                <TextBox 
                                    Grid.Row="0" 
                                    Grid.Column="1" 
                                    Margin="10"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Center" 
                                    Text="{Binding CustomMemeFolder.Keyword}" />
                                <TextBlock 
                                    Grid.Row="1" 
                                    Grid.Column="0" 
                                    Margin="10"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center" 
                                    FontSize="14" 
                                    Text="Folder Path" />
                                <TextBox 
                                    Grid.Row="1" 
                                    Grid.Column="1" 
                                    Margin="10" 
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Center" 
                                    Text="{Binding CustomMemeFolder.FolderPath}" />
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </StackPanel>
        <Border 
            Grid.Row="1" 
            Background="{DynamicResource PopupButtonAreaBGColor}"
            BorderBrush="{DynamicResource PopupButtonAreaBorderColor}" 
            BorderThickness="0,1,0,0">
            <StackPanel 
                HorizontalAlignment="Center" 
                Orientation="Horizontal">
                <Button MinWidth="140" Margin="10,0,5,0" Click="OnCancelButtonClick" Content="Cancel" />
                <Button MinWidth="140" Margin="5,0,10,0" Click="OnConfirmButtonClick" Content="Confirm"
                        Style="{DynamicResource AccentButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>